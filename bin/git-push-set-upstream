    #!/usr/bin/env bash
    set -euo pipefail

    if [ "$#" -lt 1 ]; then
      echo "Usage: git-push-set-upstream <branch> [remote]" >&2
      exit 1
    fi

    branch="$1"
remote="${2:-origin}"
git push -u "$remote" "$branch"

